import { VT323_400 } from "@/fonts";
import React from "react";

type Props = {};

export default function page({}: Props) {
  return (
    <div className={`font-mono text-lg`}>
      const hardwork = () :Promise{"<"}results{">"} ={">"} {"{"}
    </div>
  );
}
