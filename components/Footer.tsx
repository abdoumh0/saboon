import React from "react";

type Props = {};

export default function Footer({}: Props) {
  return (
    <footer className="absolute w-full bottom-0 font-mono italic text-xs text-[#2fd0a8] text-center py-1 bg-[#a82fd0]">
      Drifting along the learning curve
    </footer>
  );
}
