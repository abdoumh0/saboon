import React, { useEffect } from "react";

type Props = {};

export default function Footer({}: Props) {
  return (
    <footer className="footer fixed w-full bottom-0 font-mono italic text-sm md:text-lg text-[#2fd0a8] text-center py-1 bg-[#a82fd0]">
      {"const hardwork = () :Promise<results> => {...}"}
    </footer>
  );
}
